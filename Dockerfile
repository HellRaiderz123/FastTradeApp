# This root Dockerfile is kept for reference only.
# Use docker-compose.yml to run the full stack:
#
#   docker compose up --build
#
# See backend/Dockerfile and web/Dockerfile for individual service builds.
